---
phase: 01-theory-framework-and-single-cell-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - simulations/hh_cable_cell.py
  - simulations/neuron_reference/run_neuron_model279.py
  - simulations/neuron_reference/parameter_table.md
  - analysis/convergence_results.npz
  - analysis/lts_validation_results.npz
  - analysis/spindle_validation_results.npz
  - analysis/crossval_brian2_neuron.npz
  - figures/convergence_dt.pdf
  - figures/convergence_ncomp.pdf
  - figures/lts_threshold.pdf
  - figures/spindle_oscillation.pdf
  - figures/crossval_waveforms.pdf
interactive: false

conventions:
  membrane_potential: "V_m = V_intracellular - V_extracellular; resting = -65 mV"
  current_sign: "outward positive; C_m dV/dt = -I_Na - I_K - I_L - I_T - I_h - I_NaP + I_gap + I_ext"
  h_variable: "fraction NOT inactivated; h_inf(-65 mV) = 0.596 for I_Na"
  lfp_observable: "local field potential (NOT scalp EEG); kernel needed for cable morphology"
  reversal_potentials: "E_Na = +55 mV; E_K = -90 mV; E_Ca = +120 mV; E_L = -70 mV; E_h = -43 mV"
  temperature: "T_target = 37°C; Q10 corrections applied to all gating kinetics"
  compartments: "cable length <= 50 µm per compartment; lambda_electrotonic/10 rule; minimum N_comp = 10"
  gap_junction: "I_gap_i = g_j * (V_i - V_j); g_j in nS; outward-positive from cell i"

dimensional_check:
  V_LTS: "[mV]; pass criterion: -70 to -60 mV (i.e., -65 ± 5 mV)"
  I_T_half_activation: "[mV]; pass criterion: -59 to -55 mV (i.e., -57 ± 2 mV)"
  f_spindle: "[Hz]; pass criterion: 7-14 Hz"
  f_burst: "[Hz]; pass criterion: 100-400 Hz (intra-burst instantaneous frequency)"
  waveform_RMS: "[mV]; pass criterion: < 0.1 mV vs. NEURON reference"
  spike_timing: "[ms]; pass criterion: < 0.1 ms vs. NEURON reference"
  compartment_length: "[µm]; pass criterion: <= 50 µm per compartment"

approximations:
  - name: "Q10 temperature correction for I_T gating"
    parameter: "tadj_T = 2.5^((37-24)/10) = 2.5^1.3 ≈ 3.40"
    validity: "Standard mammalian temperature correction for Ca2+ channels; T_ref = 24°C from Huguenard & McCormick (1992)"
    breaks_when: "if NEURON ModelDB 279 uses different T_ref — check MOD files and use ModelDB value for cross-validation"
    check: "Experiment 4 cross-validation will detect any Q10 mismatch as spike timing deviation > 0.1 ms"
  - name: "Q10 temperature correction for I_Na, I_K (standard HH)"
    parameter: "tadj_NaK = 3.0^((37-6.3)/10) = 3.0^3.07 ≈ 28.0"
    validity: "Original Hodgkin-Huxley 1952 squid axon T_ref = 6.3°C; mammalian target = 37°C"
    breaks_when: "if ModelDB 279 uses different T_ref for Na/K — use ModelDB value"
    check: "NEURON cross-validation in Experiment 4"
  - name: "Exponential Euler integration for gate variables"
    parameter: "dt = 0.025 ms; first-order method"
    validity: "Confirmed stable for HH gating at dt = 0.025 ms by Experiment 1A convergence study"
    breaks_when: "dt > 0.05 ms (sodium m-gate tau < 0.1 ms near threshold)"
    check: "Experiment 1A: spike timing at dt = 0.025 ms vs. reference dt = 0.005 ms must be < 0.1 ms"
  - name: "Point-source current per compartment for LFP"
    parameter: "compartment length <= 50 µm; point-source approximation"
    validity: "Negligible error vs. line-source correction at l_comp = 50 µm"
    breaks_when: "l_comp > 100 µm (then line-source correction needed)"
    check: "Experiment 1B: LFP error at N_comp = 10 vs. N_comp = 40 must be < 5%"

estimated_execution:
  total_minutes: 90
  breakdown:
    - task: 1
      minutes: 20
      note: "Environment verification, ModelDB 279 download, parameter table fill"
    - task: 2
      minutes: 30
      note: "Brian2 HH cable implementation + convergence experiments 1A/1B"
    - task: 3
      minutes: 25
      note: "LTS threshold (Exp 2) + spindle oscillation (Exp 3) validation"
    - task: 4
      minutes: 15
      note: "NEURON cross-validation (Exp 4) + summary figures"

patterns_consulted:
  insights: []
  error_patterns: []
  adjustments_made:
    - "Explicit Q10 correction table required before implementation (prevents largest known error source at this step)"
    - "Parameter table from ModelDB 279 MOD files must be filled BEFORE writing any Brian2 code (prevents unit-mismatch errors)"

contract:
  scope:
    question: "Does the Brian2 multicompartment HH cable cell with I_T, I_h, I_NaP reproduce McCormick & Huguenard (1992) LTS threshold, spindle frequency, and waveform to the tolerances required for Phase 2?"
    in_scope:
      - "Brian2 SpatialNeuron HH cable cell implementation with I_Na, I_K, I_L, I_T, I_h, I_NaP"
      - "Q10 temperature correction to 37°C for all gating kinetics"
      - "Numerical convergence validation (dt and spatial)"
      - "LTS threshold and spindle frequency physiological validation"
      - "NEURON cross-validation against ModelDB accession 279"
    out_of_scope:
      - "LFP Green's function kernel — validated in 01-03"
      - "Brian2CUDA GPU stability — tested in 01-03"
      - "Gap junction coupled network — Phase 2"
  claims:
    - id: "claim-hh-validated"
      statement: "Brian2 SpatialNeuron HH cable cell (>= 10 compartments; I_Na, I_K, I_T, I_h, I_NaP; Q10-corrected to 37°C) reproduces: LTS threshold -65 ± 5 mV; spindle frequency 7-14 Hz; intra-burst rate 100-400 Hz; I_T half-activation -57 ± 2 mV; waveform RMS < 0.1 mV vs. NEURON at matching parameters."
      deliverables: ["deliv-hh-code", "deliv-parameter-table", "deliv-validation-figures"]
      acceptance_tests: ["test-convergence-dt", "test-convergence-spatial", "test-lts-threshold", "test-spindle", "test-crossval"]
      references: ["Ref-McCormick-1992", "Ref-Huguenard-1992", "Ref-ModelDB-279"]
  deliverables:
    - id: "deliv-hh-code"
      kind: "code"
      path: "simulations/hh_cable_cell.py"
      description: "Brian2 SpatialNeuron HH cable cell implementation"
      must_contain:
        - "ASSERT_CONVENTION: V_m = V_in - V_ex; outward-positive; h = fraction NOT inactivated"
        - "I_T = g_T * m_T^2 * h_T * (V_m - E_Ca); half-activation -57 mV"
        - "I_h = g_h * m_h * (V_m - E_h); E_h = -43 mV"
        - "I_NaP = g_NaP * m_NaP * (V_m - E_Na)"
        - "Q10 corrections applied: tadj_T = 2.5^1.3 for I_T; tadj_NaK = 3.0^3.07 for Na/K"
        - "SpatialNeuron with N_comp >= 10; compartment length <= 50 µm"
        - "Gate initialization to steady-state at V = -65 mV before any protocol"
    - id: "deliv-parameter-table"
      kind: "table"
      path: "simulations/neuron_reference/parameter_table.md"
      description: "Parameter table extracted from ModelDB 279 MOD files; used for Brian2/NEURON matching"
      must_contain:
        - "g_Na_max (S/cm2) from MOD file source"
        - "g_K_max (S/cm2) from MOD file source"
        - "g_T_max (S/cm2) from MOD file source"
        - "g_h_max (S/cm2) from MOD file source"
        - "g_NaP_max (S/cm2) or absent notation"
        - "E_Na, E_K, E_Ca, E_h, C_m, R_a values"
        - "T_ref and Q10 for I_T gating"
        - "Cable length, diameter, N_compartments (nseg)"
    - id: "deliv-validation-figures"
      kind: "figure"
      path: "figures/"
      description: "Validation figures for all four experiments: convergence, LTS threshold, spindle oscillation, Brian2 vs. NEURON cross-validation"
      must_contain:
        - "figures/convergence_dt.pdf: spike timing error vs. dt (4 levels); O(dt) convergence visible"
        - "figures/convergence_ncomp.pdf: LFP error vs. N_comp at 5 distances"
        - "figures/lts_threshold.pdf: V_m(t) trace showing LTS rebound; dV/dt threshold crossing marked"
        - "figures/spindle_oscillation.pdf: V_m(t) for 2000 ms showing >= 5 burst episodes; PSD with spindle peak annotated"
        - "figures/crossval_waveforms.pdf: Brian2 vs. NEURON V_m traces overlaid for Protocol A and B"
  references:
    - id: "Ref-McCormick-1992"
      kind: "paper"
      locator: "McCormick D.A. & Huguenard J.R. (1992) J. Neurophysiol. 68(4):1384-1400"
      role: "benchmark"
      why_it_matters: "Primary parameter source for assembled thalamic relay cell model including I_NaP; benchmark LTS threshold and spindle waveforms"
      applies_to: ["deliv-hh-code"]
      must_surface: true
      required_actions: ["read", "use", "cite"]
    - id: "Ref-Huguenard-1992"
      kind: "paper"
      locator: "Huguenard J.R. & McCormick D.A. (1992) J. Neurophysiol. 68(4):1373-1383"
      role: "method"
      why_it_matters: "Kinetic equations for I_T, I_h from voltage-clamp; I_T half-activation -57 mV is the primary validation target"
      applies_to: ["deliv-hh-code"]
      must_surface: true
      required_actions: ["read", "use", "cite"]
    - id: "Ref-ModelDB-279"
      kind: "prior_artifact"
      locator: "ModelDB accession 279 at https://modeldb.yale.edu/279 — McCormick & Huguenard (1992) NEURON code"
      role: "benchmark"
      why_it_matters: "Authoritative NEURON implementation for cross-validation; MOD files define exact parameters for Brian2 matching"
      applies_to: ["deliv-parameter-table", "deliv-hh-code"]
      must_surface: true
      required_actions: ["use", "compare"]
    - id: "Ref-Steriade-1993"
      kind: "paper"
      locator: "Steriade M., McCormick D.A. & Sejnowski T.J. (1993) Science 262:679-685"
      role: "benchmark"
      why_it_matters: "Empirical benchmark: LTS threshold ~-65 mV; spindle 7-14 Hz; intra-burst 100-400 Hz (Fig. 1-3)"
      applies_to: ["claim-hh-validated"]
      required_actions: ["compare", "cite"]
    - id: "Ref-Destexhe-1994"
      kind: "paper"
      locator: "Destexhe A. et al. (1994) J. Neurophysiol. — TRN cell model; ModelDB accession 3670"
      role: "must_consider"
      why_it_matters: "TRN-specific I_T parameters; use if relay-cell parameters (McCormick 1992) produce systematic disagreement with TRN physiology"
      applies_to: ["deliv-hh-code"]
      required_actions: ["read"]
  acceptance_tests:
    - id: "test-convergence-dt"
      subject: "claim-hh-validated"
      kind: "convergence"
      procedure: "Run Experiment 1A from EXPERIMENT-DESIGN.md: 4 Brian2 runs at dt = [0.005, 0.010, 0.025, 0.050] ms; same HH cable, same stimulus; measure spike time t_AP at each dt. Compute |t_AP(0.025) - t_AP(0.005)|."
      pass_condition: "|t_AP(dt=0.025 ms) - t_AP(dt=0.005 ms)| < 0.1 ms. Richardson extrapolation error < 0.05 ms. O(dt) convergence visible in the 3 converged levels."
      evidence_required: ["deliv-hh-code"]
    - id: "test-convergence-spatial"
      subject: "claim-hh-validated"
      kind: "convergence"
      procedure: "Run Experiment 1B: 4 Brian2 runs at N_comp = [5, 10, 20, 40]; same total cable length 500 µm; measure LFP error at 5 distances vs. N_comp=40 reference."
      pass_condition: "LFP relative error at N_comp=10 < 5% at all 5 observation distances. N_comp=5 shows > 10% error (confirming sensitivity of the test)."
      evidence_required: ["deliv-hh-code"]
    - id: "test-lts-threshold"
      subject: "claim-hh-validated"
      kind: "benchmark"
      procedure: "Run Experiment 2: hyperpolarize to -90 mV for 500 ms; release; measure V_m at first dV/dt > 10 mV/ms while V < -40 mV. Repeat for 5 holding voltages and 4 holding durations."
      pass_condition: "V_LTS in [-70, -60] mV for primary protocol (-90 mV hold, 500 ms). Intra-burst instantaneous frequency 100-400 Hz. I_T m-gate half-activation: V_half in [-59, -55] mV (from steady-state gate curve)."
      evidence_required: ["deliv-hh-code"]
    - id: "test-spindle"
      subject: "claim-hh-validated"
      kind: "benchmark"
      procedure: "Run Experiment 3: 2000 ms simulation with I_bias in burst mode; detect bursts; compute f_spindle = 1/mean_inter_burst_interval; compute PSD using Welch with 8192-sample segments (4.9 Hz resolution)."
      pass_condition: "f_spindle in [7, 14] Hz. PSD peak in 3-20 Hz band exceeds background (20-50 Hz) by > 5:1 (advisory). g_h = 0 eliminates spindle (confirming I_h dependence)."
      evidence_required: ["deliv-hh-code"]
    - id: "test-crossval"
      subject: "claim-hh-validated"
      kind: "cross_method"
      procedure: "Run Experiment 4: identical parameters in Brian2 and NEURON (ModelDB 279 values); Protocol A (AP waveform) and Protocol B (LTS rebound); compute spike timing difference and waveform RMS on 0.005 ms grid."
      pass_condition: "Spike timing: |t_AP,Brian2 - t_AP,NEURON| < 0.1 ms. Waveform RMS < 0.1 mV. LTS threshold agreement < 2 mV."
      evidence_required: ["deliv-hh-code", "deliv-parameter-table"]
  forbidden_proxies:
    - id: "fp-point-neuron"
      subject: "deliv-hh-code"
      proxy: "Implementing a single-compartment (point neuron) model and using it for LFP computation or as the Phase 2 building block"
      reason: "The LFP kernel requires spatially distributed transmembrane currents along the cable; a point neuron produces a trivial monopole that does not reflect morphology and is inconsistent with the Hales framing"
    - id: "fp-no-q10"
      subject: "deliv-hh-code"
      proxy: "Using gating kinetics directly from published room-temperature parameters without Q10 temperature correction to 37°C"
      reason: "Without Q10 correction, gates are ~3x too slow at 37°C, producing wrong LTS threshold, wrong spindle frequency, and invalid cross-validation against NEURON"
    - id: "fp-qualitative-spindle"
      subject: "claim-hh-validated"
      proxy: "Claiming spindle validation based on visual inspection of bursting behavior without measuring f_spindle from burst detection and PSD"
      reason: "Visual inspection cannot distinguish 7-14 Hz (spindle) from 3-7 Hz (delta) or 15-25 Hz (beta) bursting; the go/no-go criterion requires a measured frequency in [7, 14] Hz"
  uncertainty_markers:
    weakest_anchors:
      - "ModelDB 279 may not include I_NaP; if absent, must add from McCormick & Huguenard (1992) paper Appendix and document the modification"
      - "Q10 for I_T: RESEARCH.md notes T_ref = 24°C from Huguenard & McCormick (1992) dissociated cells; if ModelDB 279 uses T_ref = 36°C (guinea pig slice), tadj changes from 3.40 to ~1.1 — CHECK MOD FILES first"
      - "TRN vs. relay cell parameters: current plan uses McCormick & Huguenard (1992) relay cell model as validation target; Destexhe 1994 TRN parameters will differ; executor must decide which parameter set to implement and which to validate against"
    disconfirming_observations:
      - "If V_LTS falls outside [-70, -60] mV: most likely cause is wrong Q10 correction or wrong I_T half-inactivation voltage; check h_T_inf formula and T_ref before debugging further"
      - "If f_spindle < 5 Hz: I_h time constant is too long or g_h_max is too small; check tau_h Q10 correction"
      - "If Brian2 vs. NEURON spike timing > 0.5 ms: first check unit conversions (nA vs. µA, ms vs. s, mV vs. V) — this is the most common cause"
---

<objective>
Implement and validate the single HH cable cell (>= 10 compartments; I_Na, I_K, I_L, I_T, I_h, I_NaP; Q10-corrected to 37°C) in Brian2, cross-validate against NEURON ModelDB 279, and confirm all go/no-go criteria (GO-01 through GO-08) from the experiment design.

Purpose: The Phase 2 multi-cell syncytium requires a validated single-cell building block. If the single cell does not reproduce McCormick & Huguenard (1992) LTS threshold and spindle frequency, the gap-junction coupled network will not produce the target oscillations, and Phase 2 becomes uninformative. The NEURON cross-validation is the authoritative correctness check.

Output: simulations/hh_cable_cell.py (validated Brian2 model); parameter_table.md (ModelDB 279 extracted values); five validation figures; NPZ data archives for all experiments.
</objective>

<execution_context>
@./.claude/get-physics-done/workflows/execute-plan.md
@./.claude/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md
@.gpd/CONVENTIONS.md
@.gpd/phases/01-theory-framework-and-single-cell-validation/01-RESEARCH.md
@.gpd/phases/01-theory-framework-and-single-cell-validation/01-EXPERIMENT-DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Environment verification and parameter extraction</name>
  <files>simulations/neuron_reference/parameter_table.md</files>
  <action>
    Stage 0: Verify all required tools are installed before writing any simulation code.

    Run the following verification checks and record the results:
    ```python
    import brian2; print("Brian2:", brian2.__version__)
    import brian2cuda; print("Brian2CUDA: OK")
    from neuron import h; print("NEURON: OK")
    import jax; print("JAX:", jax.__version__)
    jax.config.update("jax_enable_x64", True)
    import jax.numpy as jnp; x = jnp.array([1.0]); print("JAX float64 mode: OK")
    import numpy as np; print("NumPy:", np.__version__)
    import scipy; print("SciPy:", scipy.__version__)
    import matplotlib; print("Matplotlib:", matplotlib.__version__)
    ```
    If Brian2CUDA import fails, document the failure and continue — Brian2CUDA is tested separately in 01-03 (Experiment 6). The single-cell validation (this plan) runs on CPU.

    Download ModelDB accession 279 from https://modeldb.yale.edu/279 (or https://senselab.med.yale.edu/modeldb/showmodel?model=279).
    Extract and examine all .mod and .hoc files. Fill in the parameter table:

    | Parameter | Source file and line in ModelDB 279 | Value |
    |-----------|-------------------------------------|-------|
    | g_Na_max (S/cm²) | | |
    | g_K_max (S/cm²) | | |
    | g_L (S/cm²) | | |
    | g_T_max (S/cm²) | | |
    | g_h_max (S/cm²) | | |
    | g_NaP_max (S/cm²) or absent | | |
    | E_Na (mV) | | |
    | E_K (mV) | | |
    | E_Ca (mV) | | |
    | E_L (mV) | | |
    | E_h (mV) | | |
    | C_m (µF/cm²) | | |
    | R_a (Ohm·cm) | | |
    | T_ref (°C) for I_T | | |
    | Q10 for I_T | | |
    | T_ref (°C) for Na/K | | |
    | Q10 for Na/K | | |
    | Cable total length (µm) | | |
    | Cable diameter (µm) | | |
    | N_compartments (nseg) | | |
    | I_T half-activation voltage (mV) | | |
    | I_T half-inactivation voltage (mV) | | |
    | I_h half-activation voltage (mV) | | |

    If I_NaP is ABSENT from ModelDB 279: note this explicitly in the parameter table. The Brian2 model must still include I_NaP per the phase requirement — use McCormick & Huguenard (1992) paper Appendix values and document the source. The NEURON cross-validation (Experiment 4) will then use a modified NEURON model with I_NaP added from the same source.

    Save the complete parameter table to simulations/neuron_reference/parameter_table.md.

    CRITICAL CONVENTION CHECK: Confirm that ModelDB 279 uses the same current sign convention (outward-positive) and h-gate definition (fraction NOT inactivated) as this project. Note any differences and specify the conversion applied.
  </action>
  <verify>
    1. All required tools respond without error (document any failures — Brian2CUDA failure is non-blocking here).
    2. parameter_table.md has all 22 parameter rows filled; no "(fill in)" entries remaining.
    3. Q10 and T_ref values from ModelDB 279 recorded; if they differ from RESEARCH.md defaults (Q10_T = 2.5, T_ref_T = 24°C), the ModelDB values will be used for consistency.
    4. I_NaP status documented: present in ModelDB 279 or added from paper Appendix with source noted.
    5. Convention check completed: any sign or h-gate differences noted and conversion specified.
  </verify>
  <done>Environment verified. Parameter table at simulations/neuron_reference/parameter_table.md has all 22 rows filled from ModelDB 279 MOD files. I_NaP status resolved. Convention discrepancies documented. This table is the authoritative parameter source for all subsequent tasks in this plan.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Brian2 HH cable and run convergence experiments (Exp 1A and 1B)</name>
  <files>simulations/hh_cable_cell.py, analysis/convergence_results.npz, figures/convergence_dt.pdf, figures/convergence_ncomp.pdf</files>
  <action>
    Implement the HH cable cell in Brian2 using SpatialNeuron. Use parameters from parameter_table.md (Task 1). Do NOT use parameters from memory — use the table.

    File structure for simulations/hh_cable_cell.py:
    - Header: ASSERT_CONVENTION comment block with all project conventions
    - Section 1: Q10 correction constants (compute at file top)
    - Section 2: ThalHHCableCell class wrapping Brian2 SpatialNeuron
    - Section 3: Protocol functions (run_ap_protocol, run_lts_protocol, run_spindle_protocol)
    - Section 4: Analysis utilities (detect_spikes, detect_bursts, compute_psd, compute_lts_threshold)
    - Section 5: if __name__ == "__main__": run convergence studies

    Q10 correction block (use ModelDB 279 values if they differ from these defaults):
    ```python
    # CONVENTION: Q10 temperature corrections
    # I_T, I_h gating: Q10 = 2.5, T_ref = 24°C (Huguenard & McCormick 1992, dissociated cells)
    Q10_T = 2.5
    T_ref_T = 24.0  # °C; override with ModelDB value if different
    T_target = 37.0  # °C
    tadj_T = Q10_T ** ((T_target - T_ref_T) / 10.0)  # ≈ 3.40

    # I_Na, I_K: Q10 = 3.0, T_ref = 6.3°C (Hodgkin & Huxley 1952, squid axon)
    Q10_NaK = 3.0
    T_ref_NaK = 6.3  # °C; override with ModelDB value if different
    tadj_NaK = Q10_NaK ** ((T_target - T_ref_NaK) / 10.0)  # ≈ 28.0
    ```

    Brian2 SpatialNeuron morphology:
    ```python
    from brian2 import *
    morphology = Cylinder(length=500*um, diameter=10*um, n=N_comp)  # N_comp = 10 for production
    ```

    Key gating equations for I_T (Boltzmann form from RESEARCH.md; verify against parameter_table.md):
    - m_T_inf = 1 / (1 + exp(-(V_m + 57 mV) / 6.2 mV))
    - tau_m_T = (0.612 + 1/(exp(-(V_m + 131 mV)/16.7 mV) + exp((V_m + 15.8 mV)/18.2 mV))) ms / tadj_T
    - h_T_inf = 1 / (1 + exp((V_m + 80 mV) / 4.0 mV))
    - tau_h_T: use piecewise from McCormick & Huguenard 1992 Table 1 — if V_m < -80 mV: tau_h_T = exp((V_m + 467 mV)/66.6 mV) / tadj_T; else: tau_h_T = (28 + exp(-(V_m + 22 mV)/10.5 mV)) ms / tadj_T
    Verify half-activation: m_T_inf(-57 mV) must equal 0.50 ± 0.01.

    Gate initialization: set all gates to steady-state at V = -65 mV before any protocol run. Include 100 ms settling with no current injection. Discard settling period from all analyses.

    Experiment 1A (dt convergence — see EXPERIMENT-DESIGN.md Section 1A for full protocol):
    - Run 4 simulations at dt = [0.005, 0.010, 0.025, 0.050] ms
    - Single suprathreshold pulse at t = 120 ms (0.5 nA, 5 ms duration)
    - Record V_m at soma; detect spike time t_AP; compute |t_AP(dt) - t_AP(0.005)|
    - Plot spike timing error vs. dt on log-log axes; check for O(dt) convergence
    - Save results to analysis/convergence_results.npz

    Experiment 1B (spatial convergence — see EXPERIMENT-DESIGN.md Section 1B for full protocol):
    - Run 4 simulations at N_comp = [5, 10, 20, 40]
    - Compute simple LFP approximation for convergence test (point-source sum with test observation points at [100, 200, 500, 1000, 2000] µm from cable midpoint)
    - Note: Full JAX LFP kernel validated in 01-03; here a NumPy stub is sufficient for convergence testing
    - Compute LFP relative error at each N_comp vs. N_comp=40 reference at all 5 distances
    - Save convergence table to analysis/convergence_results.npz

    If dt = 0.050 ms shows instability (V_m diverges or oscillates): document the failure mode in the convergence results file. This confirms the upper stability boundary.
  </action>
  <verify>
    1. I_T half-activation check: compute m_T_inf(-57 mV) in Python = 1/(1 + exp(-(-57+57)/6.2)) = 1/(1 + exp(0)) = 0.5 exactly. This must match what the Brian2 model produces at V = -57 mV.
    2. dt convergence: |t_AP(0.025) - t_AP(0.005)| < 0.1 ms. Richardson extrapolation error |t_AP(0.025) - t_AP(0.010)| < 0.05 ms.
    3. Spatial convergence: LFP error at N_comp=10 < 5% at all 5 observation distances vs. N_comp=40.
    4. Energy stability: V_m(t=300 ms) within ±1 mV of -65 mV for dt = 0.025 ms (no growing oscillations).
    5. Convention check: at t=0, all gate values computed as g_inf at V=-65 mV; m_Na_inf(-65) ≈ 0.05; h_Na_inf(-65) ≈ 0.60; n_K_inf(-65) ≈ 0.32. These should be printed as a gate initialization log.
    6. Literature cross-check (review profile): confirm exponential Euler is the default Brian2 integrator for SpatialNeuron gate variables; verify Brian2 documentation or source.
  </verify>
  <done>simulations/hh_cable_cell.py written with ASSERT_CONVENTION header, Q10 corrections, all five currents (I_Na, I_K, I_L, I_T, I_h, I_NaP), and production N_comp=10 cable. Experiments 1A and 1B pass: dt=0.025 ms < 0.1 ms timing error; N_comp=10 < 5% LFP error. Convergence figures saved to figures/. Results archived to analysis/convergence_results.npz.</done>
</task>

<task type="auto">
  <name>Task 3: Physiological validation — LTS threshold (Exp 2) and spindle oscillation (Exp 3)</name>
  <files>analysis/lts_validation_results.npz, analysis/spindle_validation_results.npz, figures/lts_threshold.pdf, figures/spindle_oscillation.pdf</files>
  <action>
    Run Experiments 2 and 3 from EXPERIMENT-DESIGN.md using the validated Brian2 model from Task 2 (dt = 0.025 ms, N_comp = 10).

    EXPERIMENT 2: LTS Threshold (full protocol in EXPERIMENT-DESIGN.md Section 2):

    Step 1: Initialize to V = -65 mV; all gates at steady state; 100 ms settling.
    Step 2: Bisection search for I_hold that achieves V_m = -90 ± 0.5 mV at t = 600 ms (search range: [-0.5, -0.1] nA with tolerance 0.01 nA). Document I_hold.
    Step 3: Hold for 500 ms (I_hold applied from t = 100 ms to t = 600 ms).
    Step 4: Release (I_hold = 0); record V_m(t) for 200 ms post-release.
    Step 5: Compute dV/dt (central differences); find first time t* where dV/dt > 10 mV/ms AND V_m < -40 mV; V_LTS = V_m(t*).
    Step 6: Holding depth scan — 5 holding voltages: [-75, -80, -85, -90, -95] mV; record LTS present/absent, V_LTS, spike count.
    Step 7: Holding duration scan at -90 mV — 4 durations: [100, 200, 500, 1000] ms; record LTS present/absent, V_LTS.
    Step 8: Count intra-burst action potentials; compute instantaneous frequency between spike pairs.

    Expected results: V_LTS in [-70, -60] mV; burst 3-8 spikes at 100-400 Hz intra-burst.

    EXPERIMENT 3: Spindle Oscillation (full protocol in EXPERIMENT-DESIGN.md Section 3):

    Step 1: Scan I_bias in [0.02, 0.05, 0.08, 0.12] nA; identify value producing burst mode (not silent, not tonic).
    Step 2: 2000 ms simulation (100 ms settling + 1900 ms at burst-mode I_bias).
    Step 3: Burst detection: smooth V_m with 2 ms boxcar; threshold at -40 mV; record burst onsets; compute f_spindle = 1/mean(diff(burst_onset_times)).
    Step 4: Welch PSD with 8192-sample segments (4.9 Hz resolution); record peak frequency in 3-20 Hz band; compute ratio to background (20-50 Hz).
    Step 5: g_h_max scan at [0, 50%, 100%, 150%] of nominal; record f_spindle for each (g_h = 0 should abolish spindles).

    Save all primary results (V_m traces, burst times, PSD arrays, tables of V_LTS vs. hold conditions, tables of f_spindle vs. I_bias and g_h) to analysis/lts_validation_results.npz and analysis/spindle_validation_results.npz.

    Figures:
    - figures/lts_threshold.pdf: 4-panel figure: (a) V_m(t) for primary protocol (-90 mV hold, 500 ms); (b) dV/dt(t) showing threshold crossing at V_LTS; (c) Table of V_LTS vs. holding voltage; (d) Table of V_LTS vs. holding duration
    - figures/spindle_oscillation.pdf: 3-panel figure: (a) V_m(t) 2000 ms trace with burst episodes; (b) PSD with spindle peak annotated; (c) f_spindle vs. g_h_max showing I_h dependence

    Cross-check (review profile requirement): compare V_LTS and f_spindle against Steriade, McCormick & Sejnowski (1993) Science 262:679 Fig. 1-3 values. Document the comparison in the validation results file.
  </action>
  <verify>
    1. LTS threshold criterion: V_LTS in [-70, -60] mV for primary protocol. If outside this range, check: (a) h_T recovery — was holding time 500 ms sufficient? (b) Q10 correction applied to tau_h_T? (c) I_T half-inactivation voltage — should be ~-80 mV.
    2. Intra-burst frequency: at least 2 consecutive spikes in burst with instantaneous frequency in [100, 400] Hz.
    3. LTS voltage-dependence: LTS absent at -75 mV holding (insufficient de-inactivation), present at -85 mV and deeper.
    4. Spindle frequency: f_spindle in [7, 14] Hz for optimal I_bias.
    5. I_h dependence: g_h = 0 must abolish or severely disrupt spindle (f_spindle either absent or outside 7-14 Hz band).
    6. Dimensional check: all burst times in ms; V_LTS in mV; I_bias in nA; f_spindle in Hz. Unit errors are the most common failure mode.
    7. Literature cross-check: V_LTS agrees with Steriade 1993 to within ±5 mV; f_spindle agrees with Steriade 1993 to within ±2 Hz.
  </verify>
  <done>Experiments 2 and 3 complete. V_LTS confirmed in [-70, -60] mV. f_spindle confirmed in [7, 14] Hz. I_h dependence confirmed (g_h = 0 disrupts spindle). Intra-burst frequency confirmed in [100, 400] Hz. All five go/no-go criteria GO-03 through GO-06 met. Figures saved. Results archived.</done>
</task>

<task type="auto">
  <name>Task 4: NEURON cross-validation (Exp 4) and go/no-go table</name>
  <files>simulations/neuron_reference/run_neuron_model279.py, analysis/crossval_brian2_neuron.npz, figures/crossval_waveforms.pdf</files>
  <action>
    Run Experiment 4 from EXPERIMENT-DESIGN.md Section 4: Brian2 vs. NEURON cross-validation.

    Step 1: Download NEURON ModelDB 279 if not already done (from Task 1). Run the NEURON model as-is (do not modify HOC/MOD files except to add I_NaP if absent, per Task 1 documentation).

    Prepare a Python wrapper simulations/neuron_reference/run_neuron_model279.py that:
    - Loads ModelDB 279 NEURON model
    - Runs Protocol A (AP waveform): 500 ms simulation; step 1.0 nA at t=100 ms for 5 ms
    - Runs Protocol B (LTS rebound): 800 ms simulation; -0.3 nA from t=100 ms to t=600 ms; release; record rebound
    - Saves V_m(t) arrays to disk in NPZ format (keys: "t_ms", "V_NEURON_mV")

    Run the same protocols with the Brian2 model from Task 2 using exactly the ModelDB 279 parameter values (from parameter_table.md — NOT the exploratory parameters used in Tasks 2-3). Save output as "V_Brian2_mV".

    Cross-validation metrics (computed in Python after both runs):
    - Protocol A spike timing: |t_AP,Brian2 - t_AP,NEURON| in ms
    - Protocol A waveform RMS: interpolate both traces to 0.005 ms grid; compute RMS(V_Brian2 - V_NEURON) over 50 ms AP epoch
    - Protocol B LTS threshold: |V_LTS,Brian2 - V_LTS,NEURON| in mV (use same dV/dt threshold-crossing method from Experiment 2)
    - Protocol B burst spike count: Brian2 count vs. NEURON count

    If spike timing deviates by 0.1-0.5 ms: first check unit conversions before concluding physics discrepancy. Common causes:
    - Current injected as nA in Brian2 but nanoamps vs. µA convention differs with NEURON
    - Conductance in S/cm² vs. mS/cm²
    - Q10 applied differently (check T_ref in MOD files vs. Brian2 code)

    Figures/crossval_waveforms.pdf: 2-row figure:
    - Row 1: Protocol A — Brian2 (blue) and NEURON (red) V_m(t) traces overlaid for full 500 ms; inset showing AP detail
    - Row 2: Protocol B — both traces overlaid for 800 ms showing LTS rebound; inset showing LTS detail

    Compile the complete go/no-go table for Phase 2 advancement (from EXPERIMENT-DESIGN.md), filling in measured values for all 13 hard-block criteria (GO-01 through GO-13) and 3 advisory criteria. Mark each as PASS, FAIL, or N/A (for GPU criteria GO-11, GO-12, GO-13 that are in 01-03).

    Save go/no-go table to analysis/crossval_brian2_neuron.npz and print it as text output.

    Cross-check (review profile): compare Brian2 AP half-width and peak voltage against McCormick & Huguenard (1992) Table 1 or Fig. 1 values. Document the comparison.
  </action>
  <verify>
    1. Protocol A spike timing: |t_AP,Brian2 - t_AP,NEURON| < 0.1 ms. If >0.1 ms but <0.5 ms, check unit conversions before failing.
    2. Protocol A waveform RMS < 0.1 mV over AP epoch.
    3. Protocol B LTS threshold agreement: |V_LTS,Brian2 - V_LTS,NEURON| < 2 mV.
    4. Protocol B burst spike count: must agree to ±1 spike.
    5. Go/no-go table complete: all 13 hard-block criteria have measured values; items from 01-03 (GO-09 through GO-13) marked as "pending 01-03 completion."
    6. V_m traces physically reasonable: AP peak ~+40 mV; AHP ~-75 mV; LTS envelope 10-30 mV above baseline; no NaN or Inf values.
    7. Literature cross-check (review profile): AP peak voltage and half-width documented against McCormick & Huguenard (1992) Table 1 values.
  </verify>
  <done>NEURON cross-validation complete. GO-01 (dt convergence) through GO-08 (waveform RMS) all confirmed PASS. Go/no-go table compiled with measured values for criteria GO-01 through GO-08; criteria GO-09 through GO-13 marked pending 01-03. Figures and data archived. Plan 01-02 SIMU-01 and VALD-01 requirements met.</done>
</task>

</tasks>

<verification>
Overall physics consistency checks:
- I_T half-activation: m_T_inf(-57 mV) = 0.50 exactly (algebraic check)
- LTS mechanism: requires h_T recovery (hyperpolarization) then m_T activation on rebound — confirmed by dependence on holding depth and duration
- Spindle mechanism: requires I_h for termination — confirmed by g_h = 0 scan abolishing spindles
- Q10: verified tadj_T ≈ 3.40 and tadj_NaK ≈ 28.0 by numerical computation at file top
- Compartment criterion: 500 µm cable / 10 compartments = 50 µm/compartment ≤ lambda_electrotonic/10 ≈ 40-50 µm — boundary case confirmed by convergence study
- Convention consistency: ASSERT_CONVENTION line present; outward-positive currents; h = fraction NOT inactivated

Cross-checks (review profile requirement):
- V_LTS cross-checked against McCormick & Huguenard (1992) and Steriade et al. (1993)
- f_spindle cross-checked against Steriade et al. (1993) Science 262:679 Fig. 1-3
- AP waveform cross-checked against McCormick & Huguenard (1992) Table 1
- Brian2 vs. NEURON cross-validation is the primary correctness check (Experiment 4)
</verification>

<success_criteria>
1. simulations/hh_cable_cell.py exists with ASSERT_CONVENTION header, Q10 corrections, all five currents (I_Na, I_K, I_L, I_T, I_h, I_NaP), N_comp=10 production cable
2. simulations/neuron_reference/parameter_table.md complete with all 22 parameters from ModelDB 279 MOD files
3. GO-01: dt convergence — spike timing at dt=0.025 ms < 0.1 ms vs. dt=0.005 ms reference
4. GO-02: spatial convergence — LFP error at N_comp=10 < 5% at all 5 distances vs. N_comp=40
5. GO-03: V_LTS in [-70, -60] mV for primary protocol (-90 mV hold, 500 ms)
6. GO-04: I_T m-gate half-activation V_half in [-59, -55] mV (computed from gate steady-state curve)
7. GO-05: f_spindle in [7, 14] Hz from burst detection on 2000 ms trace
8. GO-06: intra-burst frequency in [100, 400] Hz (instantaneous frequency within LTS burst)
9. GO-07: spike timing Brian2 vs. NEURON < 0.1 ms for Protocol A
10. GO-08: waveform RMS Brian2 vs. NEURON < 0.1 mV for Protocol A
11. All five validation figures exist in figures/
12. Go/no-go table complete: GO-01 through GO-08 marked PASS; GO-09 through GO-13 marked pending 01-03
</success_criteria>

<output>
After completion, create `.gpd/phases/01-theory-framework-and-single-cell-validation/01-02-SUMMARY.md`
</output>
